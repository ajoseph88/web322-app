<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
  <title>Aaron Joseph's Store</title>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Tech Haven</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <!-- Always visible routes -->
        <ul class="navbar-nav">
          {{#navLink "/shop"}}Shop{{/navLink}}
          {{#navLink "/about"}}About{{/navLink}}
        </ul>

        <!-- Right-side nav -->
        <ul class="navbar-nav ms-auto">
          {{#if session.user}}
            {{#navLink "/items"}}Products{{/navLink}}
            {{#navLink "/categories"}}Categories{{/navLink}}
            {{#navLink "/items/add"}}Add Item{{/navLink}}

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle btn btn-light text-dark ms-2" href="#" role="button" data-bs-toggle="dropdown">
                <i class="bi bi-person-circle"></i> {{session.user.userName}}
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="/userHistory">User History</a></li>
                <li><a class="dropdown-item" href="/logout">Log Out</a></li>
              </ul>
            </li>
          {{else}}
            <li class="nav-item">
              <a href="/register" class="btn btn-success me-2"><i class="bi bi-person-plus"></i> Register</a>
            </li>
            <li class="nav-item">
              <a href="/login" class="btn btn-light text-dark"><i class="bi bi-box-arrow-in-right"></i> Login</a>
            </li>
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>

  <!-- Content -->
  <div class="container" style="padding-top: 90px;">
    {{{body}}}
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
